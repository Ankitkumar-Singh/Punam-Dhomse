@model AllAssignment.Models.GuestResponse

@{
    ViewBag.Title = "Thnaks";
}

@{
    try
    {
        WebMail.SmtpServer = "mail.aressindia.net ";
        WebMail.SmtpPort = 25;
        WebMail.EnableSsl = true;
        WebMail.UserName = "patilnikhil64@gmail.com";
        WebMail.Password = " ";
        WebMail.From = "rsvps@example.com";
        WebMail.Send("party-host@example.com", "RSVP Notification", Model.Name + " is " + ((Model.WillAttend ?? false) ? "" : "not") + "attending");
    }
    catch (Exception)
    {
        @:<b>Sorry - we couldn't send the email to confirm your RSVP.</b>
        }
}
<div class="container">
    <div class="card-body mt-5 text-center text-capitalize">
        <h1>Thank you, @Model.Name!</h1>
        @if (Model.WillAttend == true)
        {
            @:It's great that you're coming. The drinks are already in the fridge!
             }
        else
        {
            @:Sorry to hear that you can't make it, but thanks for letting us know.
             }
        @Html.ActionLink("Back To RspvForm", "RspvForm", new { @class = "btn btn-sm btn-primary" })
    </div>
</div>





